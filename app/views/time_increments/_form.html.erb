<%= form_for(@time_increment) do |f| %>
  <% if @time_increment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_increment.errors.count, "error") %> prohibited this time_increment from being saved:</h2>

      <ul>
      <% @time_increment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
