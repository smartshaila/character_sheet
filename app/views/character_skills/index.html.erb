<h1>Listing character_skills</h1>

<table>
  <thead>
    <tr>
      <th>Character</th>
      <th>Skill</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @character_skills.each do |character_skill| %>
      <tr>
        <td><%= character_skill.character %></td>
        <td><%= character_skill.skill %></td>
        <td><%= link_to 'Show', character_skill %></td>
        <td><%= link_to 'Edit', edit_character_skill_path(character_skill) %></td>
        <td><%= link_to 'Destroy', character_skill, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Character skill', new_character_skill_path %>
