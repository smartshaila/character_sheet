<%= form_for(@magic) do |f| %>
  <% if @magic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@magic.errors.count, "error") %> prohibited this magic from being saved:</h2>

      <ul>
      <% @magic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :item %><br>
    <%= f.collection_select(:item_id, Item.all, :id, :name, prompt: true) %>  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
