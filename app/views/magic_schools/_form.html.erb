<%= form_for(@magic_school) do |f| %>
  <% if @magic_school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@magic_school.errors.count, "error") %> prohibited this magic_school from being saved:</h2>

      <ul>
      <% @magic_school.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
